import type { InstrumentRelation, SourceRecord } from '../types.js'

/* Sources for regions added in the 2026-09 ontology review (New Zealand, South Korea). Drafted for editorial review. */
export const regionSources: SourceRecord[] = []
export const regionRelations: InstrumentRelation[] = []
